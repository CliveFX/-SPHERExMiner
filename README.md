# SPHEREx Laser Miner

Field-first SPHEREx forced-photometry miner. The first milestone is a SIMP
field smoke test that treats `T2 SIMP J013656.5+093347.3` as a normal manual
target inside a full parent SPHEREx field.

Default local cache root:

```bash
/mnt/niroseti/spherex_cache
```

Run basic environment checks:

```bash
spherex-mine doctor
```
