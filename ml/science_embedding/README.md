# Science Embedding Model

Planned ragged/set-based model for stellar spectral families, nearest-neighbor
search, unusual-object discovery, and Gaia-aware shape/absolute embeddings.

The first implementation should train on quality-good and quality-review
spectra and write `ml_outputs/science_embeddings/<model_version>/`.
